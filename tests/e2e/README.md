# Phase 1 end-to-end tests

No browser automation framework is installed. The existing
`tests/beltcon-sbts-e2e-baseline.test.mjs` is a Node contract test, not a
Playwright browser test. Browser scenarios are intentionally deferred.
