# Phase 1 integration tests

The existing repository-level integration tests remain in `tests/*.test.mjs`.
New SBTS integration scenarios belong here once a centralized bag lifecycle and
generic bag API exist. Phase 1 deliberately adds no hardware scenario.
